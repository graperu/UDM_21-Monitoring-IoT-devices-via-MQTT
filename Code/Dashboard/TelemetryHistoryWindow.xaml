<Window x:Class="UDM_21.Dashboard.TelemetryHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lịch sử Telemetry" Height="450" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="LblTitle" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Xóa lịch sử" DockPanel.Dock="Right" HorizontalAlignment="Right"
                    Padding="14,6" Background="#dc3545" Foreground="White"
                    Click="HistoryDelete_Click"/>
        </DockPanel>

        <DataGrid x:Name="DgHistory" Grid.Row="1" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Message ID" Binding="{Binding MessageId}" Width="180"/>
                <DataGridTextColumn Header="Thời gian UTC" Binding="{Binding Timestamp}" Width="190"/>
                <DataGridTextColumn Header="Loại" Binding="{Binding DeviceType}" Width="90"/>
                <DataGridTextColumn Header="Vị trí" Binding="{Binding Location}" Width="90"/>
                <DataGridTextColumn Header="Dữ liệu" Binding="{Binding DataJson}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
